# 题目五实验报告：基于 UDP 的应用层可靠传输底层架构

## 封面信息

- 学校：[待填写]
- 学院/专业：[待填写]
- 课程名称：计算机网络
- 任课教师：[待填写]
- 实验题目：题目五可靠传输与智能拥塞控制实现
- 当前实现范围：可靠传输底层架构 + Q-Learning 智能拥塞控制器
- 小组成员：[待填写]
- 学号：[待填写]
- 班级：[待填写]
- 提交日期：[待填写]

## 摘要

本项目基于原生 UDP Socket，在应用层实现可靠传输的底层协议骨架。系统通过自定义 Header 封装分组序号、发送时间戳和固定长度数据；发送端维护未确认队列，并通过独立 Timer 线程执行 RTO 定时重传；接收端返回累计 ACK；ACK 线程根据 Timestamp 计算 RTT/SRTT。同时，系统实现了 3 个重复 ACK 触发快速重传和接收端乱序缓存。在此基础上，发送端使用 Q-Learning 控制拥塞窗口，以一个控制周期内的 SRTT 趋势和丢包事件构造 6 个状态，并从保持、加 1、减半三个动作中选择下一步。最终 V7 模型将 RTT 趋势阈值保存在 Q-Table JSON 中，通过两阶段训练和多随机种子筛选获得。

## 关键词

UDP；应用层可靠传输；Header 封装；累计 ACK；RTO；Q-Learning；智能拥塞控制

## 1. 实验目标

本实验聚焦题目五中的可靠传输底层架构，目标如下：

1. 在 UDP 之上自定义应用层数据包 Header。
2. 使用序号和累计 ACK 实现基础确认机制。
3. 发送端维护未确认队列，支持 RTO 超时重传。
4. 使用多线程拆分发送、ACK 接收和定时器扫描逻辑。
5. 设计 Q-Learning 智能拥塞控制器，根据网络反馈动态调整发送窗口。
6. 支持固定窗口与 Q-Learning 控制模式对比，便于观察智能控制效果。

## 2. 系统模块

项目保留以下核心文件：

| 文件 | 作用 |
| --- | --- |
| `protocol.py` | 数据包与 ACK 的封装、解析 |
| `sender.py` | 可靠发送端，负责滑动窗口、ACK 处理、重传和 AIMD/Q-Learning 控制 |
| `receiver.py` | 接收端，负责收包、累计确认和乱序缓存 |
| `virtual_link.py` | 虚拟瓶颈链路，负责排队、队列溢出丢包和中途带宽突变 |
| `train_q_learning.py` | 多轮 Q-Learning 训练、checkpoint 与最佳模型选择 |
| `screen_q_learning.py` | 多候选、多种子、正常/带宽骤降场景统一筛选 |
| `demo_runner.py` | AIMD、Q-Learning 和可选 DQN 的一键演示与结果汇总 |

## 3. 协议格式

### 3.1 数据包格式

| 字段 | 大小 | 含义 |
| --- | --- | --- |
| Sequence Number | 4 Bytes | 分组序号 |
| Timestamp | 8 Bytes | 发送端写入的 Unix 时间戳 |
| Payload | 1024 Bytes | 固定长度数据负载 |

在代码中，数据包格式由 `protocol.py` 中的常量定义：

```python
PAYLOAD_SIZE = 1024
DATA_HEADER_FORMAT = "!Id"
ACK_FORMAT = "!i"
```

其中 `!` 表示网络字节序，`I` 表示 4 字节无符号整数，`d` 表示 8 字节浮点数时间戳。

### 3.2 ACK 格式

ACK 报文只包含一个 4 字节字段：

| 字段 | 大小 | 含义 |
| --- | --- | --- |
| ACK Number | 4 Bytes | 当前累计确认到的最大连续序号，`-1` 表示尚无连续分组 |

## 4. Header 封装

`protocol.py` 提供四个核心函数：

- `pack_data_packet(seq, timestamp, payload)`：把序号、时间戳和负载封装为 UDP 可发送的字节串。
- `unpack_data_packet(packet)`：从收到的 UDP 数据中解析序号、时间戳和负载。
- `pack_ack(ack_number)`：封装 ACK。
- `unpack_ack(packet)`：解析 ACK。

发送端每次发送新分组或重传分组时，都会重新调用 `pack_data_packet()`，确保 Header 中的时间戳对应本次发送时间。

## 5. 可靠传输机制

### 5.1 发送端未确认队列

发送端使用 `unacked` 字典保存尚未收到 ACK 的分组。每个分组记录：

- `payload`：原始负载，用于重传。
- `last_send_monotonic`：最近一次发送时刻，用于计算是否超时。
- `wire_timestamp`：写入 Header 的 Unix 时间戳。
- `transmissions`：发送次数。

### 5.2 滑动窗口

发送端主线程按当前拥塞控制窗口控制在途分组数量。固定窗口模式下该值不变；Q-Learning 模式下该值由控制器在每个 RTT 周期结束时更新：

```text
while next_seq < end_seq and len(unacked) < window_size:
    send_new_packet(next_seq)
```

这样可以避免一次性把所有 UDP 数据报发出，也为智能拥塞控制器提供直接的调节入口。

### 5.3 累计 ACK

接收端维护 `expected_seq`：

- 如果收到的 `seq == expected_seq`，说明分组按序到达，接收端推进 `expected_seq`。
- 如果收到更大的序号，说明出现乱序，接收端先缓存该序号。
- 每次处理完数据后，接收端返回 `expected_seq - 1` 作为累计 ACK。

发送端收到 ACK 后，会删除所有 `seq <= ack_number` 的未确认分组。

## 6. 定时重传

发送端启动独立 Timer 线程，每隔一小段时间扫描未确认队列：

```text
if now - last_send_monotonic >= RTO:
    retransmit(packet)
```

当检测到超时时，发送端会：

1. 使用原 Payload 重新封包。
2. 写入新的发送时间戳。
3. 更新最近发送时间。
4. 增加发送次数。
5. 重新通过 UDP Socket 发送。

由于 UDP 本身不保证可靠性，这一机制保证了丢失的分组最终可以再次发送。

## 7. 快速重传与乱序处理

基础 RTO 重传必须等待超时，恢复速度较慢。发送端 ACK 线程会统计重复累计 ACK：

```text
if duplicate_ack_count >= 3:
    retransmit(ack_number + 1)
```

当接收端先收到后续分组、但缺少当前期望序号时，会持续返回相同的累计 ACK。发送端连续收到 3 个重复 ACK 后，立即认为 `ack_number + 1` 对应分组可能丢失，并执行快速重传。

接收端使用 `buffered` 集合缓存乱序到达的序号。缺失分组到达后，接收端会连续推进 `expected_seq`，从而一次性确认已经缓存的后续分组。

## 8. Q-Learning 智能拥塞控制器设计

Q-Learning 控制器由 `sender.py` 内置实现，发送端通过 `--cc-mode qlearning` 启用。控制器不直接修改协议格式，而是每隔 1 个 RTT 统计本周期网络反馈，再通过 Q-Table 决定下一周期的 CWND。

### 8.1 状态设计

控制器严格按照题目要求，将过去 1 个 RTT 内的网络特征离散为 6 个状态：

| 状态维度 | 取值 | 含义 |
| --- | --- | --- |
| RTT 趋势 | `rtt_up` / `rtt_down` / `rtt_stable` | 当前 SRTT 相对上一控制周期上升、下降或保持稳定 |
| 丢包事件 | `loss` / `no_loss` | 本周期是否发生 RTO 或快速重传 |

因此 Q-Table 的状态集合为 `3 × 2 = 6` 个，例如 `rtt_up|loss` 表示 RTT 上升且本周期出现丢包，通常说明窗口过大或网络排队严重。V7 将趋势阈值 `rtt_trend_threshold_ratio=0.15` 写入 JSON：只有 SRTT 相对变化超过 15% 才判定为上升或下降，否则归为稳定。该阈值可以降低正常抖动导致的状态误判。

### 8.2 动作设计

动作集合为：

| 动作 | 窗口变化 |
| --- | --- |
| `0` | CWND 保持不变 |
| `1` | CWND + 1，主动探测更多带宽 |
| `2` | CWND / 2，用于响应拥塞信号 |

控制器不是每个 ACK 都立即调窗，而是在一个 RTT 周期结束时，根据本周期统计得到的状态，通过 epsilon-greedy 策略选择下一周期动作。

### 8.3 奖励函数

奖励函数采用复合形式，综合考虑吞吐量、RTT、重传和超时四个维度：

```text
R = throughput_weight × acked_packets
    - timeout_weight × timeout_events
    - retx_weight × retransmissions
    - rtt_weight × max(0, avg_rtt_ms - target_rtt_ms)
```

其中：
- `acked_packets`：本周期成功确认的分组数
- `avg_rtt_ms`：本周期平均 RTT（ms）
- `target_rtt_ms`：目标 RTT，仅惩罚超出目标的部分
- `retransmissions`：重传次数
- `timeout_events`：超时事件数

最终 V7 候选采用两阶段奖励：正常链路阶段使用 `throughput=1.1`、`timeout=20`、`retx=4.0`、`rtt=0.015`、`target_rtt=32ms`；带宽骤降阶段使用 `throughput=1.0`、`timeout=24`、`retx=4.8`、`rtt=0.018`、`target_rtt=32ms`。第二阶段进一步提高超时和重传惩罚，使模型优先学习突发拥塞后的快速恢复。

### 8.4 Q 值更新

每个 RTT 周期结束时，控制器使用标准 Q-Learning / Bellman 公式更新上一个状态动作对：

```text
Q(s, a) = Q(s, a) + alpha * (reward + gamma * max Q(s', a') - Q(s, a))
```

程序参数 `--q-alpha` 控制学习率，`--q-gamma` 控制未来奖励折扣，`--epsilon` 控制探索概率。使用 `--qtable-file artifacts/models/active/q_table.json` 时，程序会在结束时保存当前 Q 表，下一次运行可以继续学习。历史权重备份保存在 `artifacts/models/backups/`。由于单次传输不一定收敛，项目提供 `train_q_learning.py` 进行多轮训练，并通过 `--epsilon-decay` 逐轮降低探索概率。

实现中还修复了首次 Bellman 更新污染问题。控制器启动时尚未执行任何动作，因此不存在可更新的上一状态动作对；当前代码将 `last_state` 和 `last_action` 初始化为 `None`，第一控制周期只选择动作，从第二周期开始才更新实际执行过的动作。这样可避免错误地修改 `rtt_up|no_loss → hold` 的初始 Q 值。

需要说明的是，Q-Learning 的 CWND 原始曲线不一定像固定规则控制器一样平滑。训练阶段存在 epsilon-greedy 随机探索，6 个离散状态会把不同网络情况映射到同一状态，动作也只有保持、加一和减半三类，因此窗口变化可能呈现抖动或阶梯状跳变。绘图时可以保留原始曲线，同时叠加滑动平均曲线，用原始曲线展示真实反馈，用平均曲线观察整体趋势。

### 8.5 多轮训练方法与训练结果

项目使用 `train_q_learning.py` 完成多轮训练，并使用 `screen_q_learning.py` 进行统一筛选。训练器每轮启动独立发送端和接收端，保存 metrics、history 和 checkpoint；筛选器让不同候选面对相同的分组数、链路参数和随机种子，同时测试正常链路与传输中途带宽减半两种场景。

V7 不再只训练单一参数组合，而是从干净种子分别训练均衡、低延迟和高吞吐三类候选，再比较其策略。低延迟候选进入最终阶段后，继续搜索 RTT 趋势阈值 `5% / 8% / 10% / 12% / 15% / 20%`，最终选择 `15%`。

**最终候选训练配置：**

| 参数 | 正常链路阶段 | 带宽减半阶段 |
| --- | --- | --- |
| 训练轮次 | 18 | 18 |
| 每轮分组数 | 240 | 240 |
| 学习率 α | 0.025 | 0.015 |
| 折扣因子 γ | 0.92 | 0.92 |
| 初始 ε | 0.20 | 0.10 |
| ε 衰减 | 0.94/轮 | 0.94/轮 |
| 最小 ε | 0.03 | 0.02 |
| 丢包率 | 2% | 2% |
| 基础延迟 / 抖动 | 10ms / 3ms | 10ms / 3ms |
| 带宽变化 | 无 | 传输至 50% 时减半 |

**8 个随机种子筛选结果：**

| 场景 | 模型 | 吞吐量(Mbps) | 最差吞吐(Mbps) | RTT(ms) | 重传 | 超时 | 综合分 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 正常链路 | V6 | 0.573 | 0.532 | 58.32 | 10.38 | 2.62 | 0.398 |
| 正常链路 | **V7** | 0.549 | 0.513 | **37.41** | **7.75** | **2.12** | **0.454** |
| 带宽减半 | V6 | 0.392 | 0.366 | 79.95 | 22.50 | 12.00 | -0.178 |
| 带宽减半 | **V7** | **0.406** | **0.383** | **55.37** | **8.75** | **2.88** | **0.239** |

V7 在正常链路中牺牲约 4.1% 平均吞吐，但 RTT 降低约 35.9%，重传减少约 25.3%。在更重要的带宽骤降场景中，吞吐提高约 3.6%，RTT 降低约 30.7%，重传减少约 61.1%，超时减少 76.0%。V7 的所有最差综合分均保持为正，而 V6 在骤降场景的最差分为负值，说明 V7 的跨种子稳定性更好。完整结果保存在 `artifacts/training/q_fixed_v7_screening_summary.csv`。

**最佳 Q 表策略分析：**

以下为最终 active Q 表（`artifacts/models/active/q_table.json`）在各状态下的最优动作：

| 状态 | 最优动作 | Q 值 | 策略含义 |
| --- | --- | --- | --- |
| `rtt_up\|no_loss` | cwnd/2 (2) | 8.921 | RTT 明显上升时提前减半，避免排队继续增长 |
| `rtt_up\|loss` | cwnd/2 (2) | 0.281 | RTT 上升且丢包，立即减半窗口 |
| `rtt_down\|no_loss` | cwnd+1 (1) | 9.949 | RTT 下降且无丢包，继续探测带宽 |
| `rtt_down\|loss` | cwnd/2 (2) | -7.920 | 即使 RTT 下降，只要发生丢包仍减半 |
| `rtt_stable\|no_loss` | cwnd+1 (1) | 6.970 | 网络稳定无丢包，逐步增加窗口 |
| `rtt_stable\|loss` | cwnd/2 (2) | -8.774 | 稳定状态出现丢包时减半窗口 |

最终策略仍严格使用三个动作：RTT 下降或稳定且无丢包时加 1；任意状态出现丢包时减半；当 SRTT 上升超过 15% 时，即使尚未观察到丢包也提前减半。该策略以少量正常吞吐换取更低排队延迟和更稳定的拥塞恢复。

### 8.6 一键验证评估

项目提供 `demo_runner.py` 一键运行 AIMD、Q-Learning 和可选 DQN，并生成 CSV、曲线和 HTML dashboard。正式模型筛选优先使用 `screen_q_learning.py`，因为它可以批量运行多个候选和随机种子，避免用单次最好结果代表模型性能。

当前最佳模型位于 `artifacts/models/active/q_table.json`，相同内容另存为 `artifacts/models/candidates/q_table_fixed_v7_best.json`。旧 V6 模型保存在 `artifacts/models/backups/q_table_before_fixed_v7_20260612.json`，便于回滚和复现实验。

## 9. 多线程控制

发送端包含三个并发执行流：

| 执行流 | 作用 |
| --- | --- |
| 主线程 | 按窗口大小发送新分组 |
| ACK 线程 | 调用 `recvfrom()` 接收 ACK 并更新确认状态 |
| Timer 线程 | 周期检查超时分组并重传 |

这些线程共享 `unacked`、`acked_packets`、`next_seq` 等状态，因此发送端使用 `threading.Lock` 对共享数据进行互斥访问，避免并发修改导致状态不一致。

## 10. 运行方式

启动接收端：

```bash
python3 receiver.py --host 127.0.0.1 --port 9001 --initial-seq 0
```

启动发送端：

```bash
python3 sender.py --target-host 127.0.0.1 --target-port 9001 --packets 40 --window-size 8 --rto 0.2
```

发送端运行结束后会输出确认分组数、重传次数、总耗时和平均吞吐量。

使用候选 Q 表继续在线学习：

```bash
python3 sender.py --target-host 127.0.0.1 --target-port 9001 --packets 240 --window-size 1 --cc-mode qlearning --max-cwnd 64 --epsilon 0.1 --qtable-file artifacts/models/candidates/q_table_training.json
```

使用最终模型进行纯贪心评估，不更新或覆盖 Q 表：

```bash
python3 sender.py --target-host 127.0.0.1 --target-port 9001 --packets 300 --window-size 1 --cc-mode qlearning --max-cwnd 64 --epsilon 0 --q-eval --qtable-file artifacts/models/active/q_table.json
```

模拟丢包和延迟，便于观察窗口调整：

```bash
python3 receiver.py --host 127.0.0.1 --port 9001 --loss-rate 0.08 --delay-ms 20 --jitter-ms 10 --seed 1
```

多轮循环训练时建议写入候选文件，而不是直接覆盖 active：

```bash
python3 train_q_learning.py --rounds 18 --packets 240 --q-table artifacts/models/candidates/q_table_training.json --reset-q-table --q-alpha 0.025 --q-gamma 0.92 --q-epsilon 0.20 --epsilon-decay 0.94 --min-epsilon 0.03
```

多种子筛选：

```bash
python3 screen_q_learning.py --candidate active=artifacts/models/active/q_table.json --candidate candidate=artifacts/models/candidates/q_table_training.json --seeds 3,7,11,13,17 --packets 300
```

## 11. 实验结果总结

### 11.1 可靠传输机制验证

当前版本完成了题目五中可靠传输底层架构的核心实现：Header 封装、累计 ACK、滑动窗口、RTO 定时重传、多线程控制、快速重传和乱序缓存，能够清楚展示如何在 UDP 之上用应用层代码构建可靠传输基础。所有场景下分组确认率达到 100%（300/300），验证了可靠传输机制的正确性。

### 11.2 Q-Learning 拥塞控制效果

Q-Learning 智能拥塞控制器使用 6 个离散状态和 3 个动作，经过首次 Bellman 更新修复、三类候选两阶段训练、RTT 趋势阈值搜索和 8 个随机种子验证后，得到最终 V7 模型。其策略为：

- **RTT 下降/稳定且无丢包** → `cwnd + 1`
- **任意 RTT 趋势且发生丢包** → `cwnd / 2`
- **RTT 上升超过 15%，即使无丢包** → `cwnd / 2`

与 V6 相比，V7 在正常链路牺牲约 4.1% 平均吞吐，但 RTT 降低约 35.9%；在带宽减半场景中，吞吐提高约 3.6%，RTT 降低约 30.7%，重传减少约 61.1%，超时减少 76.0%。两类场景等权时，综合吞吐只下降约 1%，但平均 RTT、重传和超时均显著下降。

因此，V7 的目标不是追求某一次运行的最高吞吐，而是在保持可接受吞吐的前提下提高跨随机种子和突发拥塞场景的稳定性。当前 Q-Table JSON 同时保存 Q 值、三个动作定义和 RTT 趋势阈值，模型体积小、决策可解释，也便于复现实验和回滚。
